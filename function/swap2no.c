#include<stdio.h>
int main()
{
    //using a third var
    // int a,b,temp;
    // printf("Enter 2 no: ");
    // scanf("%d%d",&a,&b);
    // temp=a;
    // a=b;
    // b=temp;
    // printf("after swap a=%d,b=%d",a,b);

    //without using a variable

    // int a,b;
    // printf("Enter 2 no: ");
    // scanf("%d%d",&a,&b);
    // a=a+b;
    // b=a-b;
    // a=a-b;
    // printf("After swap a=%d,b=%d",a,b);
    return 0;
}